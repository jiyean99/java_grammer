package Review;

public class 자바총리뷰 {
    /// C02MethodClass 패키지
    /// C03Class & C05Scope
    // 클래스는 “설계도”로서 동일한 구조의 데이터를 재사용/확장 가능하게 만들기 위해 사용함.
    // 클래스 생성 시 static을 붙이면 “클래스 소속(공유)”이 되고, static을 빼면 “객체 소속(개별)”이 됨.
    // 클래스를 통해 복제본을 만든 것이 객체(인스턴스) 개념이며, 객체의 핵심은 각 인스턴스가 고유한 메모리/상태를 가진다는 것.
    // static이 붙으면 객체마다 고유 상태를 갖는 게 아니라, 클래스 단위로 값/로직을 공유하게 됨(객체 지향 설계와 목적이 달라짐).

    // 객체를 생성하기 위해서는 설계 자체가 “인스턴스 중심”이어야 함.
    // static을 빼면 메서드/변수는 각각 “객체의 메서드/객체의 변수(필드)”가 됨.
    // 이후 대부분 객체 기반으로 설계를 하고, 그래서 자바를 객체지향 언어라고 부름.

    // 클래스로 객체를 만들기 위해서는 new를 사용해 특정 메모리를 할당함.
    // 즉, 객체는 고유 메모리를 가지고 그 안에 값을 할당/변경하며 상태를 유지함.

    // 클래스 메서드(static)를 쓰는 경우:
    // 객체의 고유 상태를 변경/누적하는 목적이 아니라,
    // 매개변수에 따라 계산하고 결과를 리턴하는 “상태 비의존적(독립적) 로직”일 때 사용 가능.


    ///  C04PersonMain
    // 객체를 사용하는 이유
    // 1) 고유의 메모리(인스턴스마다 독립적인 상태)
    // 2) 데이터의 그룹핑(관련 데이터를 하나로 묶어 관리 용이)
    // 예: 사람을 표현할 때 name/email 등을 사람마다 따로 변수로 만들면 관리가 어려움.
    // -> Person 클래스를 만들고 객체로 생성하면 한 사람의 데이터를 하나로 묶어 관리 가능.

    // 변수 안정성을 위해 필드는 private로 선언하는 것이 표준에 가까움.
    // private면 외부에서 p1.name 같은 직접 접근이 불가해지고,
    // 다른 클래스가 임의로 값을 바꾸는 구조를 막아 시스템 안정성이 올라감(아키텍처적으로 권장).

    // 객체 생성 시 필드 초기값
    // 참조형(String 등) = null, 원시형(int 등) = 0 등 기본값으로 세팅됨.

    // p1 객체에 값을 세팅하는 방법
    // 1) setter: 값을 “변경”하려는 의도를 메서드로 드러냄. 일반적으로 getter도 함께 필요.
    // 2) 생성자: 객체 생성 시점(new)에 필드를 초기화하는 방식(초기값 세팅에 더 적합).

    // toString()이 없으면 객체 출력 시 기본적으로 클래스명@해시(주소 기반 식별값 형태)만 출력됨.


    /// C06MethodOverloading
    // 메서드 오버로딩(Overloading)
    // 메서드명은 같지만 매개변수의 개수 또는 타입이 다른 경우.
    // 예: println, sum 등 다양하게 존재.

    // 메서드 오버라이딩(Overriding)
    // 상속/인터페이스 관계에서 부모의 메서드를 자식이 재정의하는 것(시그니처 동일).

    /// C07Constructor
    // 생성자(Constructor)
    // 객체가 만들어지는 시점(new)에 자동 호출되는 “초기화용” 메서드.
    // 반환타입이 없고, 생성자명은 클래스명과 동일함.
    // 오버로딩한 생성자를 개발자가 만들면 기본생성자는 자동으로 제공되지 않으므로,
    // 기본생성자가 필요하면 직접 작성해야 함.

    // 생성자의 사용 목적
    // 객체 사용 시점에 필드를 안정적으로 초기화하기 위함.
    // setter는 변경 가능성을 열어두므로 초기 세팅은 보통 생성자를 선호함.
    // setter가 꼭 필요하다면 의도를 명확히 드러내는 이름을 사용 권장.
    // 예: setBalance(X) 보다는 transfer/deposit/withdraw(O)


    // 제네릭(Generic)
    // 제네릭에는 원시자료형은 직접 못 들어가고(Wrapper 사용), 참조형은 모두 가능.
    // 내가 만든 클래스도 제네릭 타입으로 사용할 수 있으므로,
    // List<Account>, List<Post>처럼 도메인 객체를 컬렉션으로 관리하는 경우가 매우 흔함.


    ///  C09BankService : 계좌(Account) 생성 후 잔고이체 실습 포인트
    // private Long id;
    // -> 초기값이 null인 것이 더 자연스러운 경우(“아직 부여되지 않음”)가 많아 Wrapper(Long) 사용.
    // long balance;
    // -> 초기값이 0인 것이 더 자연스러운 경우가 많아 원시형(long) 사용.

    // staticId
    // -> 객체가 생성될 때마다 클래스 내부에서 증가시키는 식으로 구현 가능.
    // -> 실무에선 보통 DB의 auto increment(또는 시퀀스 등)로 대체되며, Spring/DB 단계에서 처리.

    // setter가 필요한 경우
    // 값 변경이 실제 요구되는 상황(이름 변경, 잔고 변경 등)에서는 어쩔 수 없이 필요할 수 있음.
    // 다만 setBalance 같은 일반 setter 대신,
    // transfer/deposit/withdraw처럼 “업무 행위(서비스 의도)”를 담은 메서드로 제공하는 것을 권장.

    // 객체 메서드 사용 지향
    // a.transfer(b, 50000);  // 객체가 자기 상태/행위를 책임지는 형태(권장)
    // Account.transfer(a, b, 50000);  // 클래스 메서드로 처리(지양)


    /// C10BoardService
    // 우리 서비스의 핵심 엔티티가 무엇인지 먼저 정의해야 함.
    // 자바에서는 엔티티가 클래스/객체로 표현되고, DB에서는 테이블로 표현됨.
    // 예: 게시판 서비스의 핵심 엔티티 = 회원(Author/User), 게시글(Post)

    // Post post = new Post(title, contents, author);
    // -> Post가 작성자(Author) 객체를 직접 참조하는 아키텍처를 사용(관계 표현).

    // Author에 List<Post> postListByAuthor를 두면,
    // 작성자가 자신이 작성한 글 목록을 함께 “소유/조회”할 수 있는 구조가 됨(양방향 관계 모델링 예시).
    // 이 때 DB의 관계에서는 1:n이 되는거고 spring에서 회원의 List<Post>로 인해 one to many, 게시글의 회원객체로 인해 many to one이 됨

    /// C03Inheritance 패키지
    /// C01Inheritance
    // A 클래스를 extends한 B 클래스는 A의 변수(필드)와 기능(메서드)을 상속받을 수 있다.
    // 상속의 핵심 특징
    // 1) 오버라이딩(재정의) 가능: 부모 메서드를 자식에서 다시 구현할 수 있고, 호출 시 자식 메서드가 우선된다.
    // 2) 다형성: 부모 타입 변수로 자식 객체를 참조할 수 있다.
    //    단, “참조 변수 타입” 기준으로 사용 가능한 메서드가 제한된다(컴파일 타임 기준).
    //    즉, A 타입으로 B를 담으면 A에 선언된 메서드만 호출 가능하고, B 고유 메서드는 직접 호출 불가(다운캐스팅 필요).

    // 다형성 예시(인터페이스/상속 모두 동일한 관점)
    // Queue<Integer> myQ = new LinkedList<>();
    // -> myQ는 Queue 타입이므로 Queue에 정의된 메서드만 사용할 수 있다(LinkedList 고유 기능은 제한됨).

    // @Override
    // 자식 클래스가 부모 클래스의 메서드를 “덮어써서(재정의)” 구현한다는 것을 명시하는 어노테이션.
    // 시그니처 불일치 등 실수를 컴파일 단계에서 잡아준다.


    /// C02SuperKeyword
    // super: 부모를 가리키는 키워드
    // - super(): 부모 클래스 생성자 호출(생성자 첫 줄에서 호출됨)
    // - super.필드 / super.메서드(): 부모 멤버에 접근(자식에서 재정의된 메서드와 구분할 때 사용)


    /// C03AnimalMain 실습
    // 다형성 확인
    // Animal dog2 = new Dog();
    // -> Animal 타입 하나로 여러 자식 객체(Dog, Cat 등)를 받아 공통 로직으로 처리할 수 있다.


    /// C04ProtectedClass
    // 접근 제어자(접근 범위)
    // public > protected > default(패키지) > private
    // 실무 관례: 클래스는 public, 필드(변수)는 private로 두고 메서드로 제어(캡슐화).
    // 시험/학습에서는 각 접근 범위를 구분해서 묻는 경우가 많다.


    /// C06AbstractMain : 추상클래스와 인터페이스
    // 추상클래스(abstract class)
    // - 추상메서드 + 일반메서드 공존 가능
    // - 추상메서드는 구현체(바디)가 없다.
    // - 추상메서드가 하나라도 있으면 클래스에도 abstract를 붙여야 한다.
    // - 자식 클래스가 extends로 상속받아 추상메서드를 오버라이딩하여 구현해야 한다.

    // 인터페이스(interface)
    // - (기본 관점에서) 메서드가 전부 추상메서드인 규격(구현 강제) 역할
    // - 구현 클래스는 implements로 구현하고, 모든 추상메서드를 오버라이딩해야 한다.
    // - (참고) 자바에서는 default/static 메서드로 구현을 포함할 수도 있지만, 학습 단계에서는 “규격”으로 이해하는 것이 핵심.

    // 상속/구현 형태 요약
    // - 자식클래스 extends 추상클래스
    // - 자식클래스 implements 인터페이스

    // final 키워드
    // - 클래스에 붙이면 상속 불가(사용 빈도는 상대적으로 낮음)
    // - 변수에 붙이면 상수(재할당 불가)로 사용되어 실무에서 자주 사용됨.


    /// C04Interface 패키지
    /// C01AnimalInterface1
    // 인터페이스 간에는 extends가 가능하다.
    // 예) public interface C01AnimalInterface3 extends C01AnimalInterface1, C01AnimalInterface2 { }
    // -> 인터페이스는 다중 상속이 가능하다(여러 인터페이스를 동시에 extends).

    // 클래스 관련 정리
    // - 클래스는 다중 상속 불가능
    // - 클래스는 다중 구현 가능(implements 여러 개 가능, 오버라이딩 구현 강제)


    /// C05AnonymousLambda
    // 익명 클래스 / 람다
    // 이름 없는 구현(일회성 구현)을 빠르게 만들기 위한 문법.
    // 인터페이스(함수형 인터페이스) 구현을 간단히 표현할 때 자주 사용된다.


    /// C04StreamApi
    // Stream API: 자바의 함수형 프로그래밍 스타일을 지원하는 도구.
    // 함수형 프로그래밍 관점: input -> output 중심이며, 원본 데이터를 직접 바꾸기보다 “흐름(파이프라인)”으로 처리한다.
    // 스트림은 보통 원본 컬렉션/배열을 직접 조작하기보다, 스트림 객체(처리용 뷰/파이프라인)를 생성해 연산을 수행한다.

    // 스트림 종류 예시
    // Stream<T>, IntStream, LongStream 등
    // 처리 방식
    // - 중개연산(intermediate): filter/map/sorted 등(지연 실행, 파이프라인 구성)
    // - 최종연산(terminal): forEach/collect/count/reduce 등(실행 트리거, 스트림 소모)

    // Optional / OptionalInt 등
    // - 값이 “있을 수도/없을 수도” 있음을 명시적으로 표현하는 컨테이너.
    // - max/min 결과가 Optional인 이유: 대상이 비어있으면 “값 자체가 없음”을 표현해야 하므로 0 같은 임의값을 리턴할 수 없기 때문.
    // - 따라서 isPresent 등으로 존재 여부를 확인한 뒤 getAsInt/get 등을 사용한다(또는 다른 안전한 꺼내기 방식 사용).

    // 메서드 참조(Method Reference)
    // 하나의 메서드만 호출하는 단순 람다는 더 짧게 줄일 수 있다.
    // 예) System.out::println


    // Optional 객체 처리(값 꺼내기/대안 처리) 대표 방식 4가지
    // 1) isPresent (확인) + get()/getAsInt()
    // 2) orElse(기본값 제공)
    // 3) orElseGet(기본값을 공급하는 함수 제공)
    // 4) orElseThrow(없으면 예외 발생)
    // 자주 쓰는 패턴: isPresent로 체크 후 get(), 또는 orElseThrow로 “없으면 중단” 처리.

    // Optional은 직접 생성하는 경우보다,
    // 라이브러리/프레임워크(자바/스프링)가 Optional로 리턴해주는 값을 “어떻게 처리할지”가 더 중요하다
    // (생성 방법 몇 가지는 상대적으로 중요도가 낮음).

    // 예외 관련 개념
    // - 예외 강제 발생(throw): 의도된 오류 상황에서 흐름을 중단시키기 위함(더 진행하면 안 되는 상태).
    // - 예외 처리(try-catch): 중단 자체를 막는 목적도 있지만,
    //   실무에서는 사용자에게 적절한 메시지/응답을 전달하기 위해 처리하는 의미가 크다.
    // 대표 예: 회원가입 시 이미 존재하는 이메일 -> 강제 예외 발생 -> 예외 처리로 사용자에게 안내 메시지 반환.


    /// C06EtcClass 패키지
    /// C03EnumMain
    // Enum(열거형): 데이터 값을 통일/표준화하기 위해 사용하는 타입.
    // 예) 상태값, 카테고리, 권한 등 “정해진 값 목록”을 안전하게 관리할 때 유용.

    /// C04GenericMain
    // 제네릭: 타입을 일반화하여 재사용성을 높이는 문법.
    // 자바에서 any처럼 받을 수 있는 형태 예시: <T>, Object, List<?>
    // Optional과 유사하게 “직접 복잡하게 설계”하는 경우는 많지 않고,
    // 이미 제네릭으로 제공되는 컬렉션/라이브러리를 “정확히 사용”하는 일이 더 빈번하다.


    /// C07ExceptionFileParsing 패키지
    /// C01ExceptionBasic
    // 예외가 발생할 수 있는 코드를 try로 감싸고,
    // 실무(웹)에서는 에러 메시지/상태를 HTTP 응답 문서 형태로 만들어 프론트에 전달해야 한다.
    // 프론트는 이를 받아 alert/toast 등으로 사용자에게 안내한다.


    /// C02ExceptionAdvance
    // 예외 강제 발생 -> 적절한 예외 처리 흐름을 연습.
    // checked exception: 컴파일 타임에 처리 강제(보통 try-catch 또는 throws 필요)
    // unchecked exception: 런타임 예외(필요 시 throw new로 발생시키고, 상위에서 처리/전파)


    /// C03File
    // IO vs NIO(Non-blocking 관점)
    // NIO는 non-blocking 방식 등으로 IO 처리 효율을 높일 수 있다.
    // 논블로킹과 비동기는 동일 개념은 아니지만,
    // “다른 작업을 기다리며 멈추지 않는다”는 관점에서 유사하게 이해할 수 있다(성능상 이점).

    // synchronized(동기화)
    // - 다른 스레드의 동시 실행을 제한해 동시성 문제를 방지한다.
    // - 대신 락으로 인해 성능이 느려질 수 있다.
    // 예) StringBuffer는 synchronized 기반이라 상대적으로 느릴 수 있음.
    // 단, 동기화가 없으면 동시성 문제가 발생할 수 있으므로 트레이드오프를 이해해야 한다.

    // 실무 관점
    // 파일 읽기/쓰기 자체를 애플리케이션 서버에서 직접 많이 하기보다,
    // S3 같은 외부 스토리지를 활용하는 구조가 흔하다.

    /// C04Json
    // 전체 데이터는 문자열인데 내부적으로는 데이터테입을 각각 가짐(boolean, int 등)
    // ObjectMapper 클래스를 통해 파싱, 직렬화를 진행함
    // readValue 메서드 : 파싱(역직렬화), JSON -> 객체
    // writeValueAsString 메서드 : 직렬화, 객체 -> JSON
    // 보통 POST 요청이 들어오면 파싱을 수행하고, GET 요청이 들어오면 직렬화를 수행한다.
    // 이 때 스프링에서는 이 수행단계가 매우 반복적이라서 자동화가 되어있다.
    // 마치 프론트에서 axios.get로 받자마자 알아서 직렬화시켜줌

    // 파싱 시에 java의 리플렉션 기술을 통해 객체 생성 (private 설계에 접근이 가능하게 되고, 이때는 기본생성자와 getter가 json 데이터 형식과 동일하게 세팅해줘야함.)

    /// C05HttpJsonParsing

    /// C08Thread 패키지
    /// ThreadMain
    // 자바에서 스레드는 작업을 처리하는 주체(자원)이다.
    // 컴퓨터에서 CPU 코어가 1개의 작업을 할당하고, Java 역시 실행 시 main에 스레드를 할당하게 됨
    // 하지만 실제로 웹서비스는 단일 서비스가 아니다.


    /// C09NetWorking
    /// C01WebServerGet
    // 서버 생성 단계 : socket을 통해 사용자와 서버 사이의 접점을 만들었음 (8080이라는 단자를 만들었다고 생각해보자)
    // 스프링의 경우 while문이 없어도 서버가 계속 꺼지지 않음
    // HTTP 문서를 만든느 각종 기술은 스프링에서 학습 예정
    // 이 때 get요청중 데이터를 넘기는 경우가 있고 보통 바디에 담기보다는 url에 같이 담아서 넘긴다. ex) 검색필터를 통해 http://localhost:8080/post?title=안녕
    // 자바에서는 요청, 응답에서 아래와 같은 작업들이 어렵다.
    // 1. 사용자의 요청값을 parsing(http request 문서)
    // 2. 사용자에게 응답값 제공(http response 문서)

    /// C03DBConnect
    // JDBC, MyBatis등을 사용하여 ...
}

